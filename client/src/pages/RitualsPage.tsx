import { Rituals } from "../components/Rituals/components/Riruals";

export function RitualsPage () {   
    return (
       <Rituals />
    );
    }

    // TODO: logging in or link to sign in page if not logged in
    // else redirect to full homePage;